ORM - OBJECT RELATIONAL MAPPER

ORM É O MAPEAMENTO DO OBJETO COM AS ANNOTATIONS SEGUINDO AS COLUNAS DA TABELA.

@TABLE
ANNOTATION @TABLE SERVE PARA IDENTIFICAR O ATRIBUTO DA CLASSE COMO UMA COLUNA DA TABELA DO BANCO DE DADOS.
DENTRO DELA A OPÇÃO DE INSERIR O NOME DA COLUNA.

@ID
ANNOTATION QUE IDENTIFICA O ATRIBUTO COMO A CHAVE PRIMARIA DA TABELA.

@COLUMN
ANNOTATION @COLUMN IDENTIFICA O ATRIBUTO COMO UMA COLUNA DA TABELA. UTILIZADO QUANDO O NOME DO ATRIBUTO E O NOME DA COLUNAS NÃO SÃO IGUAIS.
